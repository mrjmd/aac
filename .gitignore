# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
.astro/
.turbo/
out/
next-env.d.ts
*.tsbuildinfo


# Environment
.env
.env.local
.env.*.local
.env.diagnostic

# Scratch — one-off diagnostic scripts and their output, never committed
tools/src/scratch/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test
coverage/
test-results/
playwright-report/

# Debug
npm-debug.log*
pnpm-debug.log*

# SQLite (if used by marketing engine)
*.db
*.db-shm
*.db-wal

# HR / Candidate docs
docs/candidate-review-*
docs/indeed-candidates-*

# Business analysis — contains financials, customer data, tax docs.
# Local only; never committed. Back up via iCloud/Dropbox separately if desired.
# Anchored to repo root so tools/src/analysis/ (code) isn't matched.
/analysis/

# Claude
.claude/
.vercel
